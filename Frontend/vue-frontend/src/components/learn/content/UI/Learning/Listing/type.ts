export type DescriptionData = {
    heading: string
    subheading: string
    image: string
    description: string
    examples: {
      example: string
      image: string
    }[]
    signs: string[]
  }